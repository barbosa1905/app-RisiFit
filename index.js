    import { registerRootComponent } from 'expo';

    import App from './App';

    // registerRootComponent calls AppRegistry.registerComponent for you
    // and ensures the root component is registered correctly.
    registerRootComponent(App);
    